%!TEX root = ../Thesis.tex
\chapter{An Appendix}

\section{Non-interactive segmentation results}
\subsection{Hyperparameter optimization}
\label{appendix:hyperparam_optimization}

\begin{table}[ht]
\centering
\caption{Best \acs{TPE} hyperparameter optimization results for \acs{CannyFill} after $\hyperparamNTrialsCannyFill$ trials.}
\begin{tabular}{cc}
\toprule
Hyperparameter & Obtained value \\
\midrule
$d_{bilateral}$ & \hyperparamDBilateralCannyFill \\
$\sigma_{bilateral}$ & \hyperparamSigmaBilateralCannyFill \\
$d_{closing}$ & \hyperparamDCannyFill \\
$\sigma_{canny}$ & \pgfmathprintnumber[fixed,sci=3]{\hyperparamSigmaCannyFill} \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[ht]
\centering
\caption{Best \acs{TPE} hyperparameter optimization results for \acs{Gaussian} after $\hyperparamNTrialsGaussian$ trials.}
\begin{tabular}{cc}
\toprule
Hyperparameter & Obtained value \\
\midrule
$d_{bilateral}$ & \hyperparamDBilateralGaussian \\
$\sigma_{bilateral}$ & \hyperparamSigmaBilateralGaussian \\
$C$ & \hyperparamCGaussian \\
$d_{gaussian}$ & \hyperparamDGaussian \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[ht]
\centering
\caption{Best \acs{TPE} hyperparameter optimization results for \acs{mSAM+pts} after $\hyperparamNTrialsmSAMpts$ trials.}
\begin{tabular}{cc}
\toprule
Hyperparameter & Obtained value \\
\midrule
$d_{bilateral}$ & \hyperparamDBilateralmSAMpts \\
$\sigma$ & \hyperparamSigmamSAMpts \\
$C$ & \hyperparamCmSAMpts \\
$d_{gaussian}$ & \hyperparamDmSAMpts \\
$n_{fgd\_pts}$ & \hyperparamNfgdmSAMpts \\
$n_{bgd\_pts}$ & \hyperparamNbgdmSAMpts \\
$d_{gap\_erosion}$ & \hyperparamDGapErosionmSAMpts \\
\bottomrule
\end{tabular}
\end{table}

\clearpage

\subsection{Raw evaluation data}
\label{appendix:non-interactive-evaluation-data}

\pgfplotstableset{
    columns/CannyFill/.style={column name=CannyFill},
    columns/Gaussian/.style={column name=Gaussian},
    columns/Otsu/.style={column name=Otsu},
    columns/mSAM+pts/.style={column name=mSAM+pts},
}


\begin{table}[ht]
\centering
\caption{Raw \acs{Dice} score data on easy images.
Label column represents the name of the document segmented, while all other columns represent \acs{Dice} scores obtained for those documents for a given model.}
\pgfplotstabletypeset[
    col sep=comma,
    fixed,
    precision=3,
    columns={label,\modelone,\modeltwo,\modelthree,\modelfour},
    columns/label/.style={
        string type,
        column name=Label
    },
    row predicate/.code={
        \pgfplotstablegetelem{\pgfplotstablerow}{difficulty}\of{\rawpivot}
        \edef\temp{\pgfplotsretval}
        \def\match{easy}
        \ifx\temp\match
        \else
            \pgfplotstableuserowfalse
        \fi
    },
    every head row/.style={before row=\toprule,after row=\midrule},
    every last row/.style={after row=\bottomrule}
]{\rawpivot}
\end{table}

\begin{table}[ht]
\centering
\caption{Raw \acs{Dice} score data on medium images.
Label column represents the name of the document segmented, while all other columns represent \acs{Dice} scores obtained for those documents for a given model.}
\pgfplotstabletypeset[
    col sep=comma,
    fixed,
    precision=3,
    columns={label,\modelone,\modeltwo,\modelthree,\modelfour},
    columns/label/.style={
        string type,
        column name=Label
    },
    row predicate/.code={
        \pgfplotstablegetelem{\pgfplotstablerow}{difficulty}\of{\rawpivot}
        \edef\temp{\pgfplotsretval}
        \def\match{medium}
        \ifx\temp\match
        \else
            \pgfplotstableuserowfalse
        \fi
    },
    every head row/.style={before row=\toprule,after row=\midrule},
    every last row/.style={after row=\bottomrule}
]{\rawpivot}
\end{table}

\begin{table}[ht]
\centering
\caption{Raw \acs{Dice} score data on hard images.
Label column represents the name of the document segmented, while all other columns represent \acs{Dice} scores obtained for those documents for a given model.}
\pgfplotstabletypeset[
    col sep=comma,
    fixed,
    precision=3,
    columns={label,\modelone,\modeltwo,\modelthree,\modelfour},
    columns/label/.style={
        string type,
        column name=Label
    },
    row predicate/.code={
        \pgfplotstablegetelem{\pgfplotstablerow}{difficulty}\of{\rawpivot}
        \edef\temp{\pgfplotsretval}
        \def\match{hard}
        \ifx\temp\match
        \else
            \pgfplotstableuserowfalse
        \fi
    },
    every head row/.style={before row=\toprule,after row=\midrule},
    every last row/.style={after row=\bottomrule}
]{\rawpivot}
\end{table}

\clearpage

\subsection{Summary statistics}

\begin{table}[ht]
\centering
\caption{Summary Statistics per Model of Dice Score on all images.}
\pgfplotstabletypeset[
    col sep=comma,
    columns={model,Dice_mean,Dice_std,Dice_std_error,Dice_n},
    columns/model/.style={string type,column name=Model},
    columns/Dice_mean/.style={fixed,precision=3,column name=Mean Dice},
    columns/Dice_std/.style={fixed,precision=3,column name=Standard deviation},
    columns/Dice_std_error/.style={fixed,precision=3,column name=Standard error},
    columns/Dice_n/.style={fixed,precision=3,column name=$n$},
    row predicate/.code={
        \pgfplotstablegetelem{\pgfplotstablerow}{difficulty}\of{\resumetable}
        \edef\temp{\pgfplotsretval}
        \def\match{all}
        \ifx\temp\match
        \else
            \pgfplotstableuserowfalse
        \fi
    },
    every head row/.style={before row=\toprule,after row=\midrule},
    every last row/.style={after row=\bottomrule}
]{\resumetable}
\end{table}

\begin{table}[ht]
\centering
\caption{Summary Statistics per Model of Dice Score on easy images.}
\pgfplotstabletypeset[
    col sep=comma,
    columns={model,Dice_mean,Dice_std,Dice_std_error,Dice_n},
    columns/model/.style={string type,column name=Model},
    columns/Dice_mean/.style={fixed,precision=3,column name=Mean Dice},
    columns/Dice_std/.style={fixed,precision=3,column name=Standard deviation},
    columns/Dice_std_error/.style={fixed,precision=3,column name=Standard error},
    columns/Dice_n/.style={fixed,precision=3,column name=$n$},
    row predicate/.code={
        \pgfplotstablegetelem{\pgfplotstablerow}{difficulty}\of{\resumetable}
        \edef\temp{\pgfplotsretval}
        \def\match{easy}
        \ifx\temp\match
        \else
            \pgfplotstableuserowfalse
        \fi
    },
    every head row/.style={before row=\toprule,after row=\midrule},
    every last row/.style={after row=\bottomrule}
]{\resumetable}
\end{table}

\begin{table}[ht]
\centering
\caption{Summary Statistics per Model of Dice Score on medium images.}
\pgfplotstabletypeset[
    col sep=comma,
    columns={model,Dice_mean,Dice_std,Dice_std_error,Dice_n},
    columns/model/.style={string type,column name=Model},
    columns/Dice_mean/.style={fixed,precision=3,column name=Mean Dice},
    columns/Dice_std/.style={fixed,precision=3,column name=Standard deviation},
    columns/Dice_std_error/.style={fixed,precision=3,column name=Standard error},
    columns/Dice_n/.style={fixed,precision=3,column name=$n$},
    row predicate/.code={
        \pgfplotstablegetelem{\pgfplotstablerow}{difficulty}\of{\resumetable}
        \edef\temp{\pgfplotsretval}
        \def\match{medium}
        \ifx\temp\match
        \else
            \pgfplotstableuserowfalse
        \fi
    },
    every head row/.style={before row=\toprule,after row=\midrule},
    every last row/.style={after row=\bottomrule}
]{\resumetable}
\end{table}

\begin{table}[ht]
\centering
\caption{Summary Statistics per Model of Dice Score on hard images.}
\pgfplotstabletypeset[
    col sep=comma,
    columns={model,Dice_mean,Dice_std,Dice_std_error,Dice_n},
    columns/model/.style={string type,column name=Model},
    columns/Dice_mean/.style={fixed,precision=3,column name=Mean Dice},
    columns/Dice_std/.style={fixed,precision=3,column name=Standard deviation},
    columns/Dice_std_error/.style={fixed,precision=3,column name=Standard error},
    columns/Dice_n/.style={fixed,precision=3,column name=$n$},
    row predicate/.code={
        \pgfplotstablegetelem{\pgfplotstablerow}{difficulty}\of{\resumetable}
        \edef\temp{\pgfplotsretval}
        \def\match{hard}
        \ifx\temp\match
        \else
            \pgfplotstableuserowfalse
        \fi
    },
    every head row/.style={before row=\toprule,after row=\midrule},
    every last row/.style={after row=\bottomrule}
]{\resumetable}
\end{table}

\section{Tool development and user study}
\subsection{Requirement analysis and first usability test}
The 5th of May a requirement analysis and first usability test of a MVP was performed with Ester through a video call.
That MVP was composed of two tools: a classical method (Gaussian), and an interactive SAM version (MobileSAM).

After trying out the tools, she gave the impression that she could probably use the outputs as draft drawings to be completed with a pen, which might save her some time.

For the classical baseline, the grainy resolution (due to the input image quality) bothered her a bit.
The output mask was also noisy at times. And not all lines were always picked up.

For the SAM version, she pointed out that it did not pick up the noisy tablet damage as much as the classical method did.
But she was worried that the interaction with SAM would take longer than simply doing strokes manually.
The box-prompt workflow was not completely successful during the test. Due to bugs, boxes mostly did not work. and when they did, the segmented forms were not useful.

\subsection{Second (small) usability test}

The 8th of May another usability test was performed on an updated MVP with Ester through a video call. Now, the box prompts worked, and the MobileSAM was replaced with SAM2.1. The segmentation quality per box-prompted sign was noticeably better than previously.
Together with Lukas Esterle, the conclusions were to make a slider for the classical model to decrease and increase granularity.
Maybe SAM use original image, preselect the boxes! - was discarded as it was not needed.

\subsection{Third usability and final acceptance test}

The 12th of May, a third usability test was conducted in person at AIAS in Århus, including the first part of the usability experiment.
As all 45 images could not be drawn all at once, the .ora exports from the tool were saved, and finished at a later time in batches by Ester on her own, for each of them recording the time it took to do so.
Due to the huge amount of time it took to process the images with the tool (especially with the SAM version), the experiment was stopped after just 3 images.